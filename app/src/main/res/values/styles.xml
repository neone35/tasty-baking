<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorSecondary</item>
        <item name="cardViewStyle">@style/CardView</item>
    </style>

    <style name="cardTitle" parent="TextAppearance.AppCompat.Subhead">
        <item name="android:layout_margin">@dimen/text_margin</item>
    </style>

    <style name="cardLabels" parent="TextAppearance.AppCompat.Headline">
        <item name="android:fontFamily">sans-serif</item>
        <item name="android:textSize">12sp</item>
        <item name="android:layout_margin">8dp</item>
    </style>

    <style name="cardNumbers" parent="cardLabels">
        <item name="android:fontFamily">sans-serif-smallcaps</item>
        <item name="android:textSize">16sp</item>
    </style>

    <style name="detailTitle" parent="TextAppearance.AppCompat.Headline">
        <item name="android:textSize">20sp</item>
        <item name="android:layout_margin">8dp</item>
    </style>

    <style name="detailBtnMargins">
        <item name="android:layout_marginLeft">8dp</item>
        <item name="android:layout_marginRight">8dp</item>
    </style>

</resources>
